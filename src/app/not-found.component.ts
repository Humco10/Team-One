import { Component } from '@angular/core';

/**
 * When this component is loaded, just load in this simple html template and 
 * that is all you have to do basically.
 */
@Component({
	template: '<h2>Page not found: Try a different URL</h2>'
})
export class PageNotFoundComponent { }