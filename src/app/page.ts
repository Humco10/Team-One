/**
 * A page object that contains all the variables a typical page will
 * contain. Right now, all that it holds is a page number, title, and
 * path to the image the page is supposed to load.
 */
export class Page {
	pageNum: number;
	title: string;
	imgPath: string;
}